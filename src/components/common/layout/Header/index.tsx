import { Header, WebsiteTitle } from "./styles";
export const HeaderSection: React.FC = () => {
  return (
    <Header>
      <WebsiteTitle to="/">React + Typescript frontend Template</WebsiteTitle>
    </Header>
  );
};
